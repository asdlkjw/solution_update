# Sisyphus Agent Guide

이 문서는 Sisyphus 시스템을 확장하거나 개선할 때, 개발 에이전트(AI)가 효율적으로 작업을 수행할 수 있도록 돕는 프롬프트 가이드와 필수 맥락 정보를 포함합니다.

## 🛠️ 에이전트를 위한 핵심 컨텍스트

에이전트에게 작업을 요청할 때 다음 항목들을 함께 전달하면 정확도가 향상됩니다.

1.  **프로젝트 구조**: `app/` 폴더 기반의 FastAPI 구조임을 명시
2.  **데이터 흐름**: 
    - 조회: MySQL (Production)
    - 처리: SQLite (Meta/Temporary)
    - 리뷰: Web UI (Human Review)
    - 반영: MySQL (Production, Only GOOD results)
3.  **환경**: `uv`를 사용한 패키지 관리, Python 3.9+
4.  **핵심 라이브러리**: FastAPI, Pydantic, PyMySQL, MathJax

## 📝 향후 개선 요청 템플릿

새로운 기능 추가나 버그 수정을 요청할 때 다음 형식을 활용하세요.

```markdown
### 1. 작업 개요
- [ ] 예: "리뷰 화면에 문제 유형(Type) 필터 추가"

### 2. 관련 파일
- `app/templates/review.html` (UI 변경)
- `app/api/results.py` (조회 로직 변경)

### 3. 상세 요구사항
- 리뷰 화면 상단에 드롭다운 메뉴 추가
- 선택한 유형에 맞는 결과만 리스트업되도록 API 쿼리 수정
- 비동기(JS)로 화면 갱신 처리

### 4. 주의사항
- 기존 단축키(1, 2, 방향키) 기능이 깨지지 않아야 함
- SQLite 쿼리 작성 시 인덱스 활용 고려
```

## 🚀 주요 개선 포인트 (Backlog)

에이전트에게 다음 작업들을 우선적으로 요청할 수 있습니다.

- **안정성 강화**:
  - LLM API 호출 실패 시 자동 재시도 로직 구현
  - 대량 데이터 처리 시 DB 커넥션 풀링 최적화
- **UI/UX 개선**:
  - 리뷰 화면에서 수정된 부분만 하이라이트(Diff) 표시 강화
  - 다중 작업 동시 진행 시 목록 관리 기능
- **통계 기능**:
  - 작업별 성공률, 에러 유형별 통계 대시보드 추가
- **테스트 자동화**:
  - API 엔드포인트에 대한 유닛 테스트 코드 작성

## 💡 프롬프트 작성 팁

- "기존 `fix_problems.py`의 로직을 참고해서..." 와 같이 기존 코드의 맥락을 연결해 주세요.
- "UI는 `index.html`의 사이버펑크 테마를 유지해줘" 처럼 스타일 일관성을 강조해 주세요.
- "수식은 항상 `$` 또는 `$$`로 감싸진 LaTeX 형식을 유지해야 해" 와 같은 도메인 지식을 명시해 주세요.
